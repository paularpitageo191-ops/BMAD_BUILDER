# Traceability
# Functional Areas: Radio Button, Text Box, Web Tables
# Source References: AC1, AC2, AC3, Test Data: empty/null inputs, Test Data: invalid email formats, Test Data: non-numeric values
# Execution Readiness: strong
# Readiness Rationale: UI/DOM evidence is concrete enough for executable UI generation.
| TC ID | Test Name | Polarity | Description | Preconditions | Steps | Expected Results | Source Ref |
|-------|-----------|----------|-------------|---------------|-------|------------------|------------|
| TC01 | Email validation – missing TLD | Negative | Verify invalid email (missing TLD) triggers validation error and no output section. | Navigate to DemoQA Elements > Text Box. Ensure #userEmail and #submit are visible. | 1. Enter 'test@domain' into #userEmail. 2. Click #submit button. 3. Observe #userEmail for validation error. 4. Check if #output section is present. | #userEmail shows red validation error (e.g., 'Please enter a valid email'). #output is not rendered or has no content. | AC1, Test Data invalid email formats |
| TC02 | Email validation – missing @ symbol | Negative | Verify invalid email (missing @) triggers validation error and no output. | Navigate to DemoQA Elements > Text Box. Ensure #userEmail and #submit are visible. | 1. Enter 'testdomain.com' into #userEmail. 2. Click #submit. 3. Wait for validation. 4. Check #userEmail for error indicator. 5. Check presence of #output. | Validation error displayed on #userEmail. #output is not displayed. | AC1, Test Data invalid email formats |
| TC03 | Email validation – empty input | Boundary | Verify empty email field blocks submission and shows validation error. | Navigate to DemoQA Elements > Text Box. Ensure all fields are empty. | 1. Leave #userEmail blank. 2. Click #submit. 3. Observe validation on #userEmail. 4. Check for #output. | #userEmail shows a 'required' or 'please fill out' validation message. #output is not displayed. | AC1, Test Data empty/null inputs |
| TC04 | Web Tables – non-numeric Age | Negative | Verify non-numeric Age blocks submission and modal remains open. | Navigate to DemoQA Elements > Web Tables. Click 'Add' to open registration modal. | 1. Enter 'abc' in Age field. 2. Fill other required fields with valid data (First Name, Last Name, Email, Salary, Department). 3. Click Submit. 4. Check if modal is still visible. 5. Check for red validation borders or error messages on Age field. | Registration modal remains open after click. Age field shows validation error (red border or inline message). No new row is added to the table. | AC2, Test Data non-numeric values |
| TC05 | Web Tables – non-numeric Salary | Negative | Verify non-numeric Salary blocks submission and modal remains open. | Navigate to Web Tables. Open registration modal. | 1. Enter '12ab' in Salary field. 2. Fill other required fields with valid data. 3. Click Submit. 4. Observe modal state and field validation. | Modal remains open. Salary field shows validation error. No row added. | AC2, Test Data non-numeric values |
| TC06 | Web Tables – empty Age field | Boundary | Verify empty Age field blocks submission and shows validation error. | Navigate to Web Tables. Open registration modal. | 1. Leave Age field empty. 2. Enter valid data in other required fields. 3. Click Submit. 4. Check if modal closes or stays. 5. Look for validation error on Age. | Modal remains open. Age field shows 'required' error (if field is mandatory). No row added. | AC2, Test Data empty/null inputs |
| TC07 | Radio Button – disabled state of No option | Negative | Verify #noRadio is disabled and cannot be interacted with. | Navigate to DemoQA Elements > Radio Button. | 1. Locate #noRadio element. 2. Check if it is disabled (disabled attribute or CSS class). 3. Attempt to click it and observe no change in selected state. | #noRadio is disabled (has 'disabled' attribute or pointer-events: none). Clicking does not change radio selection (e.g., 'Yes' remains selected if it was previously). | AC3 |
| TC08 | Radio Button – interaction with disabled No | Negative | Verify clicking disabled No option does not trigger any state change. | Navigate to Radio Button section. Optionally pre-select 'Yes' to have a selected state. | 1. Click on #noRadio. 2. Check the selected radio option (should still be 'Yes' or nothing if none selected). 3. Repeat click multiple times. 4. Check again for any selection change. | #noRadio remains disabled and never becomes selected. Any previously selected option remains selected. | AC3 |