# Traceability
| TestCase ID | Test Name | Polarity | Description | Preconditions | Steps | Expected Results | Source Reference |
|-------------|-----------|----------|-------------|---------------|-------|-----------------|-----------------|
| TC-01 | Email validation rejects input without TLD | Negative | Verify that an email missing the top-level domain triggers validation and hides output. | Navigate to https://demoqa.com/text-box | 1. Enter 'test@domain' in #userEmail<br>2. Click #submit button<br>3. Observe the email field for validation error<br>4. Check that #output is not displayed | A validation message appears (e.g., red border or inline error); #output section is not visible (display:none or empty). | AC1 |
| TC-02 | Email validation rejects input without @ | Negative | Verify that an email without the '@' symbol is rejected. | Navigate to https://demoqa.com/text-box | 1. Enter 'testdomain.com' in #userEmail<br>2. Click #submit<br>3. Check validation error on #userEmail<br>4. Confirm #output is not displayed | Validation error is shown; #output is not visible. | AC1 |
| TC-03 | Email validation rejects empty input | Negative | Verify that an empty email field blocks submission and hides output. | Navigate to https://demoqa.com/text-box | 1. Clear #userEmail field (ensure is empty)<br>2. Click #submit<br>3. Observe whether validation error appears<br>4. Check that #output is not present | No output section appears (may be validation error or simply no content); UI remains stable (no crash or unexpected behavior). | AC1 |
| TC-04 | Email validation accepts valid input and shows output | Positive | Confirm that a correctly formatted email passes validation and output is rendered. | Navigate to https://demoqa.com/text-box | 1. Enter full valid data: name, email 'test@example.com', current address, permanent address<br>2. Click #submit<br>3. Wait for #output to appear<br>4. Verify output contains 'Email:test@example.com' | #output is visible and contains the submitted data; no validation errors are shown. | AC1, AC4 |
| TC-05 | Web table rejects non-numeric Age | Negative | Verify that a non-numeric Age value blocks submission and keeps the modal open. | Navigate to https://demoqa.com/webtables; click #addNewRecordButton to open registration modal | 1. Enter non-numeric value 'abc' in #age field<br>2. Fill other fields with valid data (First Name, Last Name, Email, Salary, Department)<br>3. Click Submit button in modal<br>4. Check that modal is still visible; check #age field for validation error | Registration modal remains open; #age field shows validation error (red border/inline message); no new row is added to the table. | AC2 |
| TC-06 | Web table rejects non-numeric Salary | Negative | Verify that a non-numeric Salary value blocks submission and keeps the modal open. | Navigate to https://demoqa.com/webtables; open registration modal via #addNewRecordButton | 1. Enter valid values except #salary: fill with '12ab'<br>2. Ensure other fields have valid data<br>3. Click Submit<br>4. Verify modal is still open; check #salary field for validation error | Registration modal remains open; #salary field shows validation error; no new record in table. | AC2 |
| TC-07 | Web table rejects empty Age and Salary | Negative | Verify that empty Age and Salary fields block submission. | Navigate to https://demoqa.com/webtables; open registration modal | 1. Clear #age and #salary fields (ensure empty)<br>2. Fill other fields with valid data<br>3. Click Submit<br>4. Check that modal remains open; check for validation errors on #age and #salary | Modal stays open; validation errors appear on both empty numeric fields; no new record added. | AC2 |
| TC-08 | Radio Button No option is disabled and non-interactable | Negative | Verify that the disabled 'No' radio button cannot be selected and remains disabled. | Navigate to https://demoqa.com/radio-button | 1. Locate #noRadio input element<br>2. Assert that it is disabled (has attribute 'disabled')<br>3. Attempt to click #noRadio (using Selenium click or JavaScript click)<br>4. Check that selected radio button (if any) did not change<br>5. Check that #noRadio remains disabled | #noRadio is disabled and remains disabled after click attempt; the selected radio button (e.g., Yes or Impressive) stays selected; no UI state change occurs. | AC3 |
| TC-09 | General negative boundary on empty inputs for Elements forms | Negative | Expand coverage to cross-module empty/null handling (text box, web tables, radio button). | Have separate instances for text box and web tables pages ready; clear any previous data | 1. On text-box page: leave all fields empty and click Submit, verify no output<br>2. On web tables: try to add a record with all fields empty, verify modal stays open<br>3. On radio button: ensure disabled state still holds after navigation | Text box: no output displayed, no error messages (or general validation); Web tables: modal remains open with field-level errors; Radio button: #noRadio still disabled. | AC1, AC2, AC4 |