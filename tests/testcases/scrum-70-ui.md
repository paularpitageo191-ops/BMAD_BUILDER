# Traceability
| ID       | Name                                     | Polarity | Description                                                                 | Preconditions                                                                                                                                           | Steps                                                                                                                                                                                                                                                                                                           | Expected Results                                                                                                                                                                                | Source       |
|----------|------------------------------------------|----------|-----------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------|
| TC-NEG-1 | Text Box - Invalid Email (missing TLD)   | Negative | Invalid email (missing TLD) must trigger validation error and suppress output. | 1. Open DemoQA Text Box page (`/text-box`).<br>2. Ensure `#userEmail` is present and clear.<br>3. Ensure `#submit` is visible and enabled.               | 1. Enter `test@domain` in `#userEmail`.<br>2. Click `#submit`.                                                                                                                                                                                                                                                  | 1. `#userEmail` shows validation error (red border or "Please enter a valid email").<br>2. `#output` section is not displayed or remains empty.                                               | AC1          |
| TC-NEG-2 | Text Box - Invalid Email (missing local) | Negative | Invalid email (missing local part) must trigger validation error.           | 1. Open DemoQA Text Box page.<br>2. Clear `#userEmail` if needed.                                                                                                                                                    | 1. Enter `@domain.com` in `#userEmail`.<br>2. Click `#submit`.                                                                                                                                                                                                                                                  | 1. Validation error displayed on `#userEmail` (red border or message).<br>2. `#output` section remains hidden.                                                                                  | AC1          |
| TC-POS-1 | Text Box - Valid Email Output Displayed  | Positive | Positive control: valid email should show output and no validation error.   | 1. Open DemoQA Text Box page.<br>2. Ensure `#userEmail` and `#submit` are available.                                                                                                                                 | 1. Enter `test@example.com` in `#userEmail`.<br>2. Click `#submit`.                                                                                                                                                                                                                                             | 1. No validation error on `#userEmail`.<br>2. `#output` section is visible and contains "Name:test@example.com" (or similar).                                                                   | AC1 (implied)|
| TC-NEG-3 | Web Tables - Non-numeric Age Blocks Submit| Negative | Non-numeric Age blocks submission and modal stays open.                     | 1. Open DemoQA Web Tables page (`/webtables`).<br>2. Click `#addNewRecordButton` to open registration modal.<br>3. Ensure Age field is empty.              | 1. Enter `abc` in Age field (id=`age`).<br>2. Fill other required fields with valid data (First Name, Last Name, Email, Salary, Department).<br>3. Click `Submit` in modal (css=`[type="submit"]`).                                                                                                            | 1. Registration modal does **not** close.<br>2. Age field shows validation error (red border or "Please enter a number").<br>3. No new row added to table.                                    | AC2          |
| TC-NEG-4 | Web Tables - Non-numeric Salary Blocks Submit| Negative | Non-numeric Salary blocks submission and modal stays open.                   | 1. Open Web Tables page.<br>2. Open registration modal via `#addNewRecordButton`.<br>3. Ensure Salary field is empty.                                     | 1. Enter `12ab` in Salary field (id=`salary`).<br>2. Fill other required fields with valid data.<br>3. Click `Submit`.                                                                                                                                                                                        | 1. Modal remains open.<br>2. Salary field shows validation error (red border).<br>3. No new row added to table.                                                                                | AC2          |
| TC-NEG-5 | Web Tables - Empty Age Blocks Submission | Negative | Empty Age field should be rejected per test data guidelines.                 | 1. Open Web Tables page.<br>2. Open registration modal.<br>3. Leave Age field empty.                                                                    | 1. Ensure Age field is empty.<br>2. Fill valid data in First Name, Last Name, Email, Salary, Department.<br>3. Click `Submit`.                                                                                                                                                                                 | 1. Modal remains open.<br>2. Age field shows validation error (e.g., "This field is required").<br>3. No new row added.                                                                       | AC2, Test Data|
| TC-NEG-6 | Radio Button - 'No' Option Disabled      | Negative | Disabled radio option must not be interactable.                              | 1. Open DemoQA Radio Button page (`/radio-button`).                                                                                                                                                                    | 1. Locate `#noRadio`.<br>2. Assert element has `disabled` attribute or `isEnabled()` returns false.<br>3. Attempt to click `#noRadio` using standard click or JavaScript.<br>4. Check feedback text area (e.g., `#result` or `[class^="text-success"]`).                                                        | 1. `#noRadio` is disabled (attribute present or `isEnabled()` false).<br>2. After click attempt, no visible change: no radio selected, no confirmation message like "You have selected No".    | AC3          |
| TC-NEG-7 | Radio Button - Clicking Disabled Does Not Change State | Negative | Confirms clicking disabled option has no effect.               | 1. Open Radio Button page.<br>2. Confirm `#noRadio` is initially disabled and unselected.                                                                                                                            | 1. Click on `#noRadio` (standard action or JS click).<br>2. Verify no radio button appears selected.<br>3. Verify feedback text (e.g., `You have selected Yes`) is not updated.                                                                                                                                 | 1. No radio button is selected after click.<br>2. Feedback text remains unchanged (could be empty or previous selection).<br>3. Page does not show error or state change.                       | AC3          |