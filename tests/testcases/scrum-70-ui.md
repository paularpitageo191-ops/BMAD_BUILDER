# Traceability
# Functional Areas: Radio Button, Text Box, UI Stability, Web Tables
# Source References: AC2, AC3, AC4 – UI Stability, Radio Button Behavior (Disabled option), Responsive design best practices, Test Data: empty/null inputs, Test Data: invalid email formats, Test Data: non-numeric values (12ab), Test Data: non-numeric values (abc), Test Data: test@domain, Text Box Validation, Text Box Validation (Invalid email), Text Box Validation (Valid input), UI Validation Reference (screenshots), UI Validation Reference screenshots, Web Tables Validation, Web Tables Validation (Invalid numeric input)
# Execution Readiness: strong
# Readiness Rationale: UI/DOM evidence is concrete enough for executable UI generation.
| Test ID | Test Name | Polarity | Description | Preconditions | Steps | Expected Results | Source Ref | Functional Area | Execution Mode |
|---------|-----------|----------|-------------|---------------|-------|------------------|------------|-----------------|----------------|
| TC01 | Text Box – Invalid email (missing TLD) triggers validation error and no output | Negative | Verify that entering an email without a TLD (e.g., test@domain) triggers validation error and #output is not displayed. | Navigate to DemoQA Elements → Text Box. Ensure #userEmail and #submit are visible. | 1. Locate #userEmail input field. 2. Type 'test@domain' into #userEmail. 3. Click #submit button. 4. Check for validation error (red border, error message) on #userEmail. 5. Verify that #output section is not rendered or is empty. | Validation error appears on #userEmail. #output section is absent or empty. | AC1, Text Box Validation (Invalid email), Test Data: test@domain | Text Box | automation-selenium-java |
| TC02 | Text Box – Invalid email (special characters) triggers validation error and no output | Negative | Verify that entering an email with special characters (e.g., test@@domain.com) triggers validation error and no output. | Navigate to DemoQA Elements → Text Box. Ensure #userEmail and #submit are visible. | 1. Locate #userEmail input. 2. Type 'test@@domain.com' into #userEmail. 3. Click #submit button. 4. Check for validation error on #userEmail. 5. Verify #output is not displayed. | Validation error appears on #userEmail. #output section is absent or empty. | AC1, Test Data: invalid email formats | Text Box | automation-selenium-java |
| TC03 | Text Box – Empty email input triggers validation error and no output | Boundary | Verify that submitting with an empty email field triggers validation error and no output. | Navigate to DemoQA Elements → Text Box. Ensure #userEmail is cleared. | 1. Ensure #userEmail is empty. 2. Click #submit button. 3. Check for validation error (browser-native or custom) on #userEmail. 4. Verify #output is not displayed. | Validation error shown on #userEmail (e.g., 'Please fill out this field'). #output section is not rendered. | AC1, Test Data: empty/null inputs | Text Box | automation-selenium-java |
| TC04 | Text Box – Positive email validation with valid input displays output | Positive | Verify that entering a valid email (test@example.com) and submitting displays the output section. | Navigate to DemoQA Elements → Text Box. Clear previous inputs. | 1. Locate #userEmail and type 'test@example.com'. 2. Click #submit button. 3. Verify #output is visible and contains 'test@example.com'. | #output section is displayed and contains the submitted email. | AC1, Text Box Validation (Valid input) | Text Box | automation-selenium-java |
| TC05 | Web Tables – Non‑numeric age blocks submission and keeps modal open | Negative | Verify that entering a non‑numeric value (e.g., 'abc') in the Age field blocks submission and the registration modal remains open. | Navigate to DemoQA Elements → Web Tables. Click 'Add' button to open registration modal. | 1. Fill valid First Name, Last Name, Email, Salary (e.g., 50000), Department. 2. Enter 'abc' in Age field. 3. Click Submit button. 4. Verify modal is still visible (not dismissed). 5. Check for validation error (red border) on Age field. | Modal remains open. Age field shows validation failure. No new row added to the table. | AC2, Web Tables Validation (Invalid numeric input), Test Data: non-numeric values (abc) | Web Tables | automation-selenium-java |
| TC06 | Web Tables – Non‑numeric salary blocks submission and keeps modal open | Negative | Verify that entering a non‑numeric value (e.g., '12ab') in the Salary field blocks submission and modal stays open. | Navigate to DemoQA Elements → Web Tables. Open registration modal via 'Add' button. | 1. Fill valid First Name, Last Name, Email, Age (e.g., 30), Department. 2. Enter '12ab' in Salary field. 3. Click Submit button. 4. Verify modal remains visible. 5. Check validation indicator on Salary field. | Modal stays open. Salary field shows validation error. No new row added. | AC2, Web Tables Validation, Test Data: 12ab | Web Tables | automation-selenium-java |
| TC07 | Web Tables – Empty age field blocks submission and keeps modal open (boundary) | Boundary | Verify that leaving the Age field empty blocks submission and modal remains open. | Navigate to DemoQA Elements → Web Tables. Open registration modal. | 1. Fill valid First Name, Last Name, Email, Salary, Department. 2. Leave Age field blank. 3. Click Submit button. 4. Verify modal still open. 5. Check Age field for required-field validation. | Modal remains open. Age field shows required validation (browser tooltip or border). No row added. | AC2, Test Data: empty/null inputs | Web Tables | automation-selenium-java |
| TC08 | Radio Button – 'No' option remains disabled and does not change state on click | Negative | Verify that the 'No' radio button (#noRadio) is disabled and does not become selected when clicked. | Navigate to DemoQA Elements → Radio Button. | 1. Locate #noRadio element. 2. Verify #noRadio has disabled attribute or class. 3. Attempt to click #noRadio. 4. After click, verify #noRadio is still disabled and no selection indication appears. 5. Check that no success message (e.g., 'You have selected No') appears. | #noRadio is initially disabled. Click does not remove disabled state. No selection feedback displayed. | AC3, Radio Button Behavior (Disabled option) | Radio Button | automation-selenium-java |
| TC09 | UI Stability – Elements remain interactable under overlay obstruction | UI / Stability | Verify that interactive elements can still be accessed via scroll/visibility handling when obstructed by an overlay. | Navigate to DemoQA Elements page. Optionally add a fixed-position overlay (if not present) or use existing modal. | 1. Add a fixed overlay covering part of the page (if not already present). 2. Attempt to click an element underneath (e.g., submit button) using standard click. 3. If standard click fails, use scrollIntoView and then click. 4. Verify click action executes successfully (state change or navigation). 5. Check no UI shifts or crashes. | Element is interactable via scroll/visibility handling. Click action succeeds. Page does not freeze or throw errors. | AC4, UI Validation Reference screenshots | UI Stability | automation-selenium-java |
| TC10 | UI Stability – Elements remain interactable under browser viewport changes | UI / Stability | Verify that elements remain interactable after resizing viewport to a mobile size. | Navigate to DemoQA Elements page. Set initial viewport to 1280x720. | 1. Resize viewport to 375x667. 2. Scroll to #userEmail, verify visible and enabled. 3. Enter test value and click submit. 4. Repeat for Web Tables 'Add' button and Radio Button section. | All target elements visible after scrolling. Input and click actions succeed. UI does not clip/overlap. | AC4, Responsive design best practices | UI Stability | automation-selenium-java |