# Traceability
# Functional Areas: Radio Button, Text Box, UI Stability, Web Tables
# Source References: AC2, AC3, AC4 – UI Stability, Radio Button Behavior (Disabled option), Responsive design best practices, Test Data: empty/null inputs, Test Data: invalid email formats, Test Data: non-numeric values (12ab), Test Data: non-numeric values (abc), Test Data: test@domain, Text Box Validation, Text Box Validation (Invalid email), Text Box Validation (Valid input), UI Validation Reference (screenshots), UI Validation Reference screenshots, Web Tables Validation, Web Tables Validation (Invalid numeric input)
# Execution Readiness: strong
# Readiness Rationale: There is enough technical context to support executable coverage where the approved scope requires it.
| ID | Scenario Name | Polarity | Description | Preconditions | Steps | Expected Results | Source References | Execution Recommendation |
|----|---------------|----------|-------------|---------------|-------|------------------|-------------------|--------------------------|
| REG-01 | Invalid email (missing TLD) triggers validation error and suppresses output | Negative | Verify that an email missing a TLD (e.g., `test@domain`) is rejected: validation error appears on `#userEmail` and `#output` is not displayed. | Navigate to DemoQA Elements → Text Box. Ensure `#userEmail` and `#submit` are visible. | 1. Locate `#userEmail` input. 2. Type `test@domain`. 3. Click `#submit`. 4. Check for validation error (red border, error message) on `#userEmail`. 5. Verify `#output` is not displayed or is empty. | Validation error is present on `#userEmail`; `#output` section is absent or contains no content. | AC1, Test Data: invalid email, Text Box Validation (Invalid email) | Selenium Java – automation |
| REG-02 | Non‑numeric age blocks Web Table submission and keeps modal open | Negative | Ensure entering non‑numeric text in the Age field (e.g., `abc`) prevents submission and leaves the registration modal open. | Navigate to DemoQA Elements → Web Tables. Click `Add` to open modal. | 1. Fill valid First Name, Last Name, Email, Salary (e.g., `50000`), Department. 2. Enter `abc` in Age field. 3. Click `Submit`. 4. Verify modal is still visible. 5. Check for validation indicator on Age field. | Modal remains open; Age field shows validation failure (red border, error message); no new row added. | AC2, Web Tables Validation, Test Data: non‑numeric values (abc) | Selenium Java – automation |
| REG-03 | 'No' radio button stays disabled and does not change state on click | Negative | Validate that the `#noRadio` element is disabled and that clicking it does not remove the disabled state or show a selection message. | Navigate to DemoQA Elements → Radio Button. | 1. Locate `#noRadio`. 2. Verify disabled attribute or `disabled` class. 3. Attempt to click using Selenium WebDriver click. 4. After click, verify `#noRadio` is still disabled. 5. Check that no success message appears. | `#noRadio` is initially disabled; click does not remove disabled state; no selection feedback displayed. | AC3, Radio Button Behavior (Disabled option) | Selenium Java – automation |
| REG-04 | UI stability under overlay obstruction – elements remain interactable | UI | Confirm that interactive elements (e.g., submit button) remain clickable even when partially covered by a fixed overlay, using scroll/visibility handling. | Navigate to DemoQA Elements page. Optionally inject a fixed overlay (if not already present) over a target element. | 1. Add a fixed‑position overlay covering part of the page (or use an existing modal). 2. Attempt to click the element underneath using standard click. 3. If blocked, use `scrollIntoView` and then click. 4. Verify click action succeeds (state change or navigation). 5. Confirm no page crash or error. | Element is interactable via scroll/visibility handling; click executes correctly; page remains stable. | AC4 – UI Stability, UI Validation Reference (screenshots) | Selenium Java – automation |
| REG-05 | Positive email validation – valid input displays output | Functional | Ensure the happy path for email validation still works after negative tests: valid email should render the output section. | Navigate to DemoQA Elements → Text Box. Clear any previous input. | 1. Locate `#userEmail` and type `test@example.com`. 2. Click `#submit`. 3. Verify `#output` is visible and contains the email. | `#output` section is displayed and contains the submitted email. | AC1, Text Box Validation (Valid input), UI Validation Reference | Selenium Java – automation |