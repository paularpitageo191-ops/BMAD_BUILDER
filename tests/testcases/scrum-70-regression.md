# Traceability
| Testcase ID | Scenario Name | Polarity | Description | Preconditions | Steps | Expected Results | Source References |
|-------------|---------------|----------|-------------|---------------|-------|------------------|-------------------|
| REG-01 | Web Tables CRUD after negative validation | Regression | Verify that after submitting invalid Age/Salary, the table still supports valid CRUD operations. | User is on DemoQA Elements page; Web Tables section visible; previous negative tests (non-numeric Age/Salary) executed in same session | 1. Click ‘Add’ to open registration modal.<br>2. Enter valid data: First Name=Alice, Last Name=Smith, Email=alice@test.com, Age=28, Salary=45000, Department=QA.<br>3. Click Submit.<br>4. Verify new row appears in table.<br>5. Click Delete icon on that row. | New row is added with correct values; row is successfully removed from table. | Approved scope: Regression scenario #7; AC2 |
| REG-02 | Text Box valid email still accepted after invalid attempts | Regression | Ensure that after submitting an invalid email (missing TLD), a subsequent valid email submission works correctly. | User on Text Box section; previous invalid email submission performed in same session | 1. Enter valid data: Full Name=John, Email=john.doe@example.com, Current Address=123 Main St, Permanent Address=456 Oak Ave.<br>2. Click Submit.<br>3. Check #output section. | #userEmail no longer shows validation error; #output section is displayed and contains the submitted email. | AC1; Approved scope: Positive test #3 |
| REG-03 | Radio Button – other options remain selectable after attempt on disabled “No” | Regression | Verify that clicking the disabled “No” option does not lock or affect the selection of other radio buttons. | User on Radio Button section; previous attempt to click #noRadio performed (via JS click if needed) | 1. Click “Yes” radio button.<br>2. Verify “Yes” is selected (dot shown).<br>3. Click “Impressive” radio button.<br>4. Verify “Impressive” is selected and “Yes” is deselected. | “Yes” and “Impressive” can be toggled normally; no console errors or UI glitches. | AC3; Screenshots reference |
| REG-04 | UI stability under overlay obstruction | Regression | After introducing a modal overlay (e.g., Web Tables registration modal), other page elements remain interactable with scroll/visibility. | User on Elements page; registration modal is open (from Web Tables ‘Add’ click) | 1. Scroll to Text Box section while modal is open.<br>2. Enter valid email in #userEmail field.<br>3. Click #submit. | Submitting while modal is open should not crash the page; #output section may be blocked but email validation error should appear (if invalid) or modal interaction remains. (Focus on absence of JavaScript errors and no page freeze.) | AC4; Stability risk |