# Traceability
# Functional Areas: Radio Button, Text Box, Web Tables
# Source References: AC1, AC2, AC3, Boundary analysis, Boundary: negative numbers, Test Data: invalid email formats, Test Data: non-numeric values
# Execution Readiness: strong
# Readiness Rationale: There is enough technical context to support executable coverage where the approved scope requires it.
| testcase_id | scenario_name | polarity | description | preconditions | test_steps | expected_results | source_refs |
|-------------|---------------|----------|-------------|---------------|------------|------------------|-------------|
| REG-AC1-01 | Regression: Invalid email (no TLD) blocks form submission and hides output | Regression | Ensure that the email validation for missing Top-Level Domain (AC1) continues to prevent form submission and does not expose the output section. | DemoQA Elements page is loaded; Text Box section is visible; #userEmail field is empty | 1. Navigate to /elements/text-box<br>2. Enter 'test@domain' in #userEmail<br>3. Click #submit | #userEmail shows validation error (e.g., `field-error` class or `invalid` attribute); #output div remains hidden (`display: none` or not present); no success message appears | AC1, Test Data (invalid email formats) |
| REG-AC2-01 | Regression: Non-numeric Age in Web Table blocks submission and keeps modal open | Regression | Confirm that entering non-numeric characters in the Age field (AC2) still prevents the modal from closing and does not create a new row. | DemoQA Elements page loaded; Web Tables section visible; Registration modal is closed | 1. Click 'Add' to open registration modal<br>2. Enter 'John' in First Name<br>3. Enter 'Doe' in Last Name<br>4. Enter 'abc' in Age<br>5. Enter 'jdoe@example.com' in Email<br>6. Click modal Submit button | Modal remains open; Age field shows validation error (red border / error text); no new row added to the table; form fields retain entered values | AC2, Test Data (non-numeric values) |
| REG-AC3-01 | Regression: Disabled 'No' radio option remains non-interactable | Regression | Verify that the 'No' radio button (AC3) stays disabled and cannot be clicked or trigger any state change. | DemoQA Elements page loaded; Radio Button section visible; default selection state (any) | 1. Locate #noRadio element<br>2. Check `disabled` attribute or CSS class<br>3. Attempt to click #noRadio (using `.click()` or JavaScript click)<br>4. Observe any output or selection change | #noRadio has `disabled` attribute (or `pointer-events: none`); clicking does not change the selected radio button; no message appears; 'Yes' and 'Impressive' remain unaffected | AC3 |